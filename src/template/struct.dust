{#structs}
  exports.{displayName} = function {displayName}(bytes) {
    {#field}
      this.{name} = {>{slot.type.template} field={.} /};
    {/field}
  };

  {displayName}.prototype.isComplete = function () {

  }
{/structs}
