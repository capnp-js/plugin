define(['capnp-js/reader/Arena', './scope'], function (
                         Arena,     scope) {

    var constants = {};

    {#.nodes}
        {>"constants/node" recurTarget="struct"/}
    {/.nodes}

    return constants;
});
