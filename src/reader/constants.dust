define(['capnp-js/builder/Allocator', 'capnp-js/reader/index', './rScope'], function (
                          Allocator,            reader,            scope) {

    var constants = {};
    var allocator = new Allocator();

    {#.nodes}
        {>"constants/node" recurTarget="struct"/}
    {/.nodes}

    return constants;
});
