define(['capnp-js/reader/Arena', './rScope'], function (
                         Arena,      scope) {

    var constants = {};

    {#.nodes}
        {>"constants/node" recurTarget="struct"/}
    {/.nodes}

    return constants;
});
