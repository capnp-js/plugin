{?.type.meta}
    {#.type}
        {! Update context for recursion. !}
        {@eq key=.meta value="list"}reader.lists.pointer({>"readers/list"/}){/eq}
        {@eq key=.meta value="struct"}scope['{.id}']._LIST{/eq}
    {/.type}
{:else}
    reader.lists.{.type}
{/.type.meta}
