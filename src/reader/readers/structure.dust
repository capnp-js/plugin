(function (types, parentScope, allocator) {
    var Structure = types["{.id}"];

    {#.nodes}{>"readers/node"/}(types, Structure, allocator);{/.nodes}
    {>"readers/fields" base="Structure"/}

    parentScope.{.name} = Structure;
})
