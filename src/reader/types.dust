define(['capnp-js/reader'], function (reader) {

    var types = {};

    {! Populate id's only--pet-names come later in the pipeline. !}

    {#.nodes}
        {>"types/structure"/}
        {>"types/enumeration" recurTarget="struct"/}
    {/.nodes}

    return types;
});
