define(['./rTypes'{?.imports},{@imports file="rTypes"/}{/.imports}], function (
            types {?.imports},{#.imports}file{$idx}{@sep},{/sep}{/.imports}{/.imports}) {

    {#.imports}
        {#.typeIds fileId=$idx}
            types["{.}"] = file{fileId}["{.}"];
        {/.typeIds}
    {/.imports}

    return types;
});
