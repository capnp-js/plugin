{@eq key=.meta value="struct"}
    {^.type.parameters}
        types['{.id}'] = builder.structure(rTypes['{.id}']);
    {:else}
        types['{.id}'] = builder.metastructure(rTypes['{.id}']);
    {/.type.parameters}

    {#.nodes}
        {>"bTypes/structure"/}
    {/.nodes}
{/eq}
