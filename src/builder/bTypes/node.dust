{@eq key=.meta value="struct"}
    {?.parameters}
        types['{.id}'] = builder.generic(rTypes['{.id}'])
        {?.hash}.bindRootScope(){/.hash};
    {:else}
        types['{.id}'] = builder.structure(rTypes['{.id}']);
    {/.parameters}

    {#.nodes}
        {>"bTypes/structure"/}
    {/.nodes}
{/eq}
